apiVersion: v1
kind: Service
metadata:
  name: nginx-exporter-service
  namespace: default
  labels:
    app: nginx-exporter

spec:
  type: ClusterIP
  selector:
    app: nginx-exporter

  ports:
    - name: http
      port: 9113
      targetPort: 9113