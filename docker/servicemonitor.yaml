apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nginx-exporter-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: nginx-exporter
  endpoints:
  - port: http
    interval: 15s