
apiVersion: v1
kind: Service
metadata:
  name: nginx-clusterip
spec:
  type: ClusterIP
  selector:
    app: nginx           # must match the labels on your Pods
  ports:
    - name: http         # (recommended) name the port for clarity
      port: 80           
      targetPort: 80     